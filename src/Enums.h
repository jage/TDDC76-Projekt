#ifndef ENUMS_H_
#define ENUMS_H_

enum PANZER_STATES
{
	PLAYER1STATE = 0,
	NETWORKSTATE,
	OPTIONSTATE,
	EXITGAME,
	FIRE,
	MENY,
	PLAYER2STATE
};

enum PANZER_IMAGE
{
	SUN,
	GROUND,
	CONCRETE
};

// some preprocessor defines
#define CANNON_WIDTH 10

#endif
