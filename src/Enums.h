#ifndef ENUMS_H_
#define ENUMS_H_

enum PANZER_STATES
{
	PLAYER1STATE = 0,
	NETWORKSTATE,
	OPTIONSTATE,
	EXITGAME,
	FIRE,
	MENY,
	PLAYER2STATE,
	INITSTATE,
	FIREEND
};

enum PANZER_IMAGE
{
	SUN,
	GROUND,
	CONCRETE,
	CANNON,
	CANNONBALL
};

enum PANZER_ALIGNMENT
{
	LEFT,
	RIGHT,
	CENTER
};

enum PANZER_FONT
{
	LAZY32,
	LAZY26
};

// some preprocessor defines
#define CANNON_WIDTH 10

#endif
